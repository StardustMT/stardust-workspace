# Sibling repos — each is its own git repo, not tracked by the workspace.
/stardust-pit/
/stardust-core/
/stardustmt.github.io/

# Temporary migration source.
/_wiki-source/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
!stardust.code-workspace

# Local env
.env
.env.local

# Org profile is its own git repo (StardustMT/.github)
/.github-org/
